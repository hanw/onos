machine:
    environment:
        ONOS_ROOT: ~/onos
    java:
        version: oraclejdk8

dependencies:
    override:
        - echo "skipped" #mvn clean install -DskipTests -Dcheckstyle.skip -U -T 1C

test:
    override:
        - echo "skipped" #mvn test

deployment:
    staging:
        branch: master
        commands:
            - mkdir -p ~/Downloads
            - ONOS_ROOT=~/onos ./tools/build/onos-package
            - ./waltz/bin/do_release
